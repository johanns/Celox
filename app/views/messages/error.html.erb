<%= render 'banner', :heading => t('.heading'), :info1 => t('.info1') %>

<div class="container">
	<div style="margin-top: 120px">
		<div class="alert-message block-message error">
			<%= t('.not_found') %>
		</div>
		<div class="well" style="margin-top: 10px">
			<input value="<%= t('.new_message') %>" class="btn btn-large btn-success" type="button" onclick="location.href='<%= escape_javascript(APP_URL) %>'" />
		</div>
	</div>
</div>
